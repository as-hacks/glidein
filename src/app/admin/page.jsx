export const metadata = {
  title: 'Admin Dashboard | Glidein Studios',
  robots: {
    index: false,
    follow: false,
  },
};

import Admin from '@/components/Admin';

export default function AdminPage() {
  return <Admin />;
}
